#include <stdlib.h>
#include <string.h>
#include <stdio.h>

FILE* file;

unsigned static int program[100000];

typedef enum int mem_t;
typedef enum bool prog_t;

bool opsring() {
  typedef struct {
    POS1,
  } opsring;

  return opsring;
}

